{
  "name": "meme-maker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "invoke-local": "sudo sls invoke local -f mememaker --logger --docker --path mocks/request.json",
    "invoke": "sudo sls invoke -f mememaker --docker --logger --path mocks/request.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hapi/joi": "^17.1.1",
    "axios": "^0.20.0"
  }
}
